import React from 'react';
import { AppRegistry } from 'react-native';
import App from './src/App';

const appNotas = props => (
  <App />
)

AppRegistry.registerComponent('AppNotas', () => appNotas);
